<div class="center jumbotron">
<%= form_for(@state) do |form| %>
  <% if @state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@state_name.errors.count, "error") %> prohibited this product from being saved:</h2>
      <ul>
      <% @state_name.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
  <div class="form-group">
    <%= form.label :name %>
    <%= form.text_field :name, id: :state_name %>
  </div>
  </div>

  <div class="actions">
    <%= form.submit 'Create', class: "btn btn-primary" %>
  </div>

<% end %>
</div>